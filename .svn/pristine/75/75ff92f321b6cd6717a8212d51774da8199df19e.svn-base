package stat;

import com.business.core.entity.mix.Mix;
import com.business.core.entity.user.UserRun;
import com.business.core.mongo.BaseMongoDaoSupport;
import com.business.core.utils.CollectionUtil;
import com.business.scheduler.jobs.stat.UserGrowthLossJob;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.mongodb.core.query.Criteria;
import org.springframework.data.mongodb.core.query.Query;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import java.util.List;

/**
 * Created by Cheri on 2015/9/6.
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = {"classpath*:applicationContext.xml"})
public class UserGrowthLossJobTest extends BaseMongoDaoSupport {

    @Autowired
    private UserGrowthLossJob userGrowthLossJob;

    @Test
    public void findNewAddUserCountTest() {
//        System.out.println(userGrowthLossJob.findNewAddUserCount());
    }

    @Test
    public void allMixDownloadCount() {
//        List<Mix> allMix = getRoutingMongoOps().findAll(Mix.class);
//        Integer totalCount = 0;
//        for (Mix mix: allMix) {
//            totalCount += mix.getDownloadCount();
//        }

    }


}
